* Create project directory structure
** Create file lib.js in src and libTest.js in test

*   node ./wc.js file1
    write test for default file case
    Create function wc in lib.js = { whcih will accept a filepath and fs}
    extra fucntions like countLines, countWords and countCharacter
    handle case of multiple spaces

*   node ./wc.js -l file1

    write test for linecount of single file.
    write test for parseInput
    write parseInput function in parseInput.js
    modify wc function to accept an object

    modify parseInput to handle multiple arguments and return as an array of options
    multiple arguments are handled

*     node ./wc.js file1 file2
    write code for handling multiple files

*   handle cases of giving options as seperate arguments
    create function in parseInput to merge all the seperate functions into one
    
